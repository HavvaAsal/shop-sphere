const initialState = {
  items: [],
  total: 0
};

const cartReducer = (state = initialState, action) => {
  switch (action.type) {
    // Cart işlemleri...
    default:
      return state;
  }
};

export default cartReducer; 